# ONTINUITY PUNCH LIST — resolved working view
Source of truth for current state. The chronological folds in `live/agent_queue.md` remain the append-only **archive/ledger** (nothing removed there); this file is the **resolved view** distilled from it — every real item reconciled to DONE / IN-PROGRESS / OPEN, deduplicated, closed items citing the commit or receipt that closed them. Regenerated by distillation; the queue stays the raw record. Last resolved: 2026-06-29 PM (Oracle process SHELVED — no consumer; coordinator kept as the live build target toward the single-pane goal; latency+consumer findings recorded). Prior: 2026-06-29 (Oracle step-1 mailbox surgery DEPLOYED live; three-role leash design settled against source + corrected; read_repo/manual/bootstrap-gate housekeeping closed). Prior: 2026-06-28 (06-21 mailbox backlog archived + control inbox cleared; Worker11/Worker22 booted).

Data model: each item has `state` (DONE | IN-PROGRESS | OPEN), and OPEN/IN-PROGRESS items carry a `tier` (HIGH | MED | MINOR) and, where designed, a risk-tier (SAFE | REVIEW | RISK per the sign-off rulings).

---
## DONE (closed — with the commit/receipt that closed it)

- **Oracle build STEP 1 — mailbox schema + contract (DEPLOYED LIVE). Oracle steps 2+ SHELVED 2026-06-29 (no consumer).** 2026-06-29. Step 1 added question/answer kinds, corr_id/citations/confidence nullable columns (additive idempotent migration), taught mailbox_send/fetch/peek/you_there to carry them, per-call kinds filter. Chain: worker22 authored -> worker11 found 2 defects -> worker22 fixed (Option B) -> worker11 signed off (d9d6ccf8) -> operator authorized -> control installed (write_file+restart) + verified live (corr_id persists+round-trips, no regression). Pre-install DB dump to PRIVATE repo (8c2bda80). Box synced to repo (commit_self c58e4cb0). Oracle spec at 9f2e4b22. **SHELVE DECISION (operator, 2026-06-29 PM): the Oracle PROCESS (steps 2+ — stand-up, trigger, boot wiring) is SHELVED — it has no consumer. Workers ground by reading directly; a pure-routing coordinator needs no adversarial grounding; the design chair cannot consume an async multi-cycle answer (that is the assertion rule's job). And latency makes it structurally async-only (2-cycle floor, 4+ sequential model calls > 60s window). The step-1 plumbing is harmless and stays. Do NOT resume without naming a concrete consumer. See gated_session_substrate.md decision banner.**
- **read_repo source-priority — RESOLVED do-not-delete.** 2026-06-29 (a28a9bec). The delete-order was disproven by the 06-14 Researcher ledger evidence (raw-cachebust is the tokenless workers' structural primary path; deleting it breaks worker grounding). KEEP the code; settled, do not relitigate.
- **Manual currency — allowlist 17->19 ops.** 2026-06-29 (10b1f233). Added mailbox_purge, backup_db to the manual list (bootstrap-gate CHECK 1 would have failed on the count mismatch).
- **Leash architecture — three roles settled against source.** 2026-06-29 (design spec live/specs/gated_session_substrate.md, 912973a7). Oracle = Tetraform session; Coordinator = deterministic code loop in shepherd's lineage (just coordinates, nothing bolted on); Governor = read-only pane (gains hands later). v1 of the note (d4a853b6) over-claimed a general substrate from a config read and was corrected after reading the loop body — the config-read-vs-loop-read lesson is folded into the assertion rule.
- **06-21 mailbox backlog archived + control inbox cleared (2026-06-28)** — the 145 queued kind=result laptop messages (S22 node-build results, 2026-06-21 13:56-16:20 UTC) jamming control's inbox were archived verbatim with metadata to live/archive/mailbox_2026-06-21_laptop_results.md (worker11, commit 93af9f85, read-back byte-identical 145/145), then drained. SECURITY: the messages contained plaintext live creds (GitHub token + DIAG_KEY); GitHub secret-scanning blocked first commit; worker11 redacted to placeholders before re-commit (archive clean; per standing operator ruling 2026-06-20 the creds are accepted pre-public risk, not a blocker). PURGE NOTE: kind-scoped purge removed 165 (the 145 queued + 20 already-consumed/done laptop results from the same session); the 20 done were already spent and are NOT in the archive (flagged, not recoverable). Channel now clean. Staged via box write_file+commit_file (REVIEW-tier, no engine deploy).
- **Mailbox result-channel fix — the all-night transport jam, ROOT-CAUSED + FIXED + PROVEN (2026-06-21)** — symptom: results couldn't be retrieved reliably for a whole session (peek/fetch returning empty, ~419 results jammed). ROOT CAUSE: seat_mailbox is a TASK-DISTRIBUTION queue (atomic claim+lease so two workers never grab the same job) being USED AS A RESULT CHANNEL — the claim/lease semantics that make distribution safe are what jammed retrieval. Four compounding effects: (1) results (kind=result,to_seat=control) pile up 'queued' because the reader uses PEEK (read-only, never marks done) — nothing drains them; (2) fetch claims OLDEST-first (ASC) while peek shows NEWEST-first (DESC) — opposite directions, new results buried behind old; (3) peek default limit=20 (max 100) showed only the oldest stuck 20; (4) blind drain loops claimed-without-acking, locking msgs under ~13min leases. FIX (3 surgical, backward-compatible, default task-claim path UNCHANGED): seat_mailbox.py (08364cbe) — mailbox_fetch accepts reply_to=<task_id> (correlated: pull MY specific result) + newest=true (drain newest-first); new mailbox_purge endpoint (bulk-clear result/note backlog). app.py (6acd4cfe) — mailbox_purge added to OP_ALLOWED (allowlist 18->19). DEPLOYED both surfaces: engine via Railway serviceInstanceDeploy+commitSha (serviceInstanceRedeploy rebuilds the PINNED/stale commit — wrong tool); BOX via write_file(seat_mailbox.py)+restart_workspace (the courier forwards mailbox ops to the BOX, not the engine — Railway deploys don't touch box disk; repo-commit != box-install). Two-party gate ran clean (MAILBOXFIX-1, author=control/signer=operator, dry-run authorized:true). PROVEN: purged 419 stale, queue clean, correlated fetch-then-ack round-trip confirmed. STANDING DISCIPLINE: fetch-by-reply_to then ACK IMMEDIATELY, never leave a dangling claim. Handoff 31a97cde.
- **Parietal + Model C provider config — finalize-hang FIXED + VERIFIED (2026-06-20)** — root cause was the two roles having no explicit provider, inheriting the shared null-model Novita fallback (404 → dead Parietal → close hangs at finalizing:true, never writes). FIX: PARIETAL→cerebras/gpt-oss-120b (operator choice; OpenAI-lineage, distinct from the GLM Challenger — separation holds), MODEL_C→novita/meta-llama/llama-3.1-8b-instruct (Cerebras llama3.1-8b was deprecated 2026-05-27 — caught before re-introducing a dead model). VERIFIED live: post-fix test session showed contract_criteria=2 (Parietal authored a contract vs 0 on the hung run), ZERO console 404s, loop adjudicates instead of hanging. Standing lesson: every role needs explicit *_URL/*_MODEL; "configured by omission via fallback" with a null PROVIDER_MODEL is a latent outage. Conversation record 00783ab9.
- **laptop_seat.py into version control** — the reverse-connection laptop seat (C:\donkeycar\laptop_seat.py: dials out to the engine mailbox, polls tasks, executes scoped run/read/write, posts results back; no inbound port) existed ONLY on the laptop disk with no repo provenance — same single-homed-source gap the box server had before commit_self. Pulled via the seat's own read op, scanned CLEAN (diag key read from config.json at runtime, never embedded — verified against the three known live secret literals), committed to live/box/laptop_seat.py (sha 4af7b8b3). Hands that build the iPad keyboard now recoverable from the repo. Single-seat commit on an unwatched non-deploying path (rubric-acceptable; flagged to operator).
- **Dweller build-status established (2026-06-20 Researcher-seat lap)** — NOT a build; the Dweller is a recurring SESSION TYPE that ranks the queue by evidence and outputs the single defended next item, gate-adjudicated ("the system decides its own head," operator 2026-06-07). Exhaustive corpus search (all 166 text files) + live DB: 3 artifacts reference it (work_products + session log showing the C1/C2/C3 ranking criteria), 0 sessions.objective, 0 established_results. Last lap grounds to 2026-06-06; none since. NOT tracked as software, NOT a punch-list build item. The "which-task-next surface for the worker pool" need is the Dweller discipline applied to the pool (see OPEN: Dweller-as-pool-surface), not a missing component. Conversation record a624fdfa.
- **backup_db op + first DB backup to private repo** — box_ops.py +/op/backup_db (62c834a0, INSTALLED via write_file+restart), engine OP_ALLOWED 17->18 (app.py 308ea384, redeployed, confirmed live). Python sqlite3 .backup + iterdump -> `.sql` text (consistent snapshot; text ships through commit_file, binary `.db` cannot — UTF-8 text ops + no sqlite3 CLI on box). RAN: 3.85MB dump -> PRIVATE repo ontinuity-intake-data backups/ontinuity_dump.sql (cbfb6220). Closes the last single-homed-DB risk for a manual run; repeatable as backup_db->commit_file. Manual scoped-ops list updated same close. AUTOMATION follow-up (periodic/auto dump-and-commit) tracked OPEN below.
- **Boot-packet permanence (recurring migration defect, structurally closed)** — decohering seat must never AUTHOR the boot artifact (root of the recurring drift). Fixed snippet CONTROL_QUICKBOOT_SNIPPET.md (e2267613), copied verbatim, points a fresh seat to CONTROL_QUICKBOOT.md (FETCH-and-VERIFY not obey). CONTROL_QUICKBOOT.md rewrite (9c2edb0b: real credential model, suppression framing cut) + hard-gate on five-doc read (d3999e66, after a cold test seat deferred the manual). Cold-tested live: fresh seat booted clean, reported real 17-op allowlist. Mirrors the worker-quickboot pattern.
- **Website papers page + logo fix** — /papers.html on-brand (959a85f5), gate-spine signature; index.html wordmark-spacing fix (flex-gap was splitting the `.o` span) + Papers nav tab (88a0304f). Live https://ontinuity.org/papers.html; site + TikTok outreach comment made public by operator.
- **seed_tenant op — hands-free client-tenant provisioning** — box_ops.py +/op/seed_tenant (repo d6ecf683, INSTALLED on box via write_file 39302b + restart), engine OP_ALLOWED 16->17 (b5cad43a). Bounded, idempotent (get-or-create user+project), ledgered, no arbitrary SQL. Two-party deploy chain ran (proposal control-seat / signoff worker-review, block seedtenant-1781530749). FIRST CLIENT TENANT SEEDED: Katie Wasserman / "SHS Emergency Shift-Coverage Tool" (user 07f75d61, project eecb5348), confirmed via SELECT relay, idempotency verified (re-run created:false). LESSON (folded to manual 32fe3f07): a new box op is NOT live until write_file-to-BOX-DISK + restart — committing box_ops.py to the repo alone leaves the box on stale on-disk code (404 until installed). Repo-commit and box-install are TWO steps (manual line 122). Cost ~an hour of wrong "Railway throttle blocks it" declarations before grounding in the corpus found the real gap.
- **you_there long-poll + no-self-sign-off filter** — stacked in seat_mailbox.py (622c7046), OP_ALLOWED 12->13 (beeb053a), manual 13 (b46c2d69). Within-turn self-draining node loop; authorship-based self-sign-off skip. Two-worker pool proven live (atomic claim, distinct draws).
- **Mailbox threat audit** — worker2 (47eb97a4). All 4 cross-seat surfaces exploitable under shared key; per-identity keys necessary-not-sufficient (must derive identity from key + stop trusting body fields). Threat-model input for the key build.
- **Closed write-loop: read_file + commit_file box ops** — box_ops.py extended (d6713827) + engine OP_ALLOWED 10->12 (8cc59f57), deployed (box hands-free via write_file+restart; engine clean on Railpack pin). read_file = bounded box read (mirror of write_file); commit_file = commit arbitrary box file to repo (generalizes commit_self). Closes the worker artifact loop: worker writes to box -> control reads back -> control commits to VC. Proven end to end with zero operator SSH.
- **Verified bootstrap gate runnable (step 1) + first live run caught its own preconditions** — gate.py (cd609bab), spec w/ CHECK 6 MECHANICS + seat-role param (de9abc73). Six checks, {control,worker}. On first run caught 2 real drifts (manual allowlist 10 vs canonical 12; no-self-poll invariant absent from manual) -> both fixed (manual b5c28125). The gate verified its own operators and found them wanting — thesis demonstrating itself. (Step 2, the courier op, remains IN-PROGRESS.)
- **Worker propose/sign-off loop proven live** — worker1 (chat seat) ran the full mailbox loop with no human relay (BOOTGATE-1 spec 37925ff3, BOOTGATE-2 build cd609bab); control verified/committed. Confirms the one-node primitive: roles emergent from mailbox item-kind, every node can propose OR verify, no fixed controller/sub architecture needed.
- **Box write-ops (write_file + commit_self) + provenance hole CLOSED** — box_ops.py (1b8052e8) + engine courier (143d8937), deployed live. commit_self verified: the box read its OWN running file_server.py (1522 lines) + seat_mailbox.py + box_ops.py and pushed them to live/box/ — repo now matches the box byte-for-byte, no transcription. Full write-hands loop closed: sandbox seat -> courier -> box writes to version control, no operator splicing. Future box source changes commit the same way.
- **Seat mailbox (seat-to-seat coordination channel)** — box blueprint seat_mailbox.py (549e678b) + engine OP_ALLOWED courier (429eb735). Five ops (send/fetch/ack/peek/reclaim), scale-ready (lineage, broadcast targets, atomic claim, lease+reclaim, corpus-pointer ref). DEPLOYED + smoke-tested live end to end through the courier; ops logged on operations_ledger. Kills operator message-routing between conversations.
- **Relay-courier `/diag/op/<name>` (sandbox-seat box hands)** — committed f295e2f, confirmed DEPLOYED live (~16:43:37 UTC June 10); round-tripped read_journal + restart_workspace to the box. The arm that gives any chat-sandbox seat real box hands without IP whitelisting.
- **Railpack Python-version deploy fix (standing)** — builds failed on mise `no precompiled python @3.13.14` (latest precompiled is 3.13.12). Fixed via Railway vars RAILPACK_PYTHON_VERSION=3.13.12 (exact) + NO_CACHE=1. Standing pin; prevents recurrence. (RAILPACK_PYTHON_VERSION=3.13 alone fails — resolves to 3.13.14.)
- **Operator deadlock-escape `/agent/stop` endpoint** — keyed stop, stopped_by lineage, 3-bounce escalation. Deploys 22–23, receipt #20.
- **Connect-time state resync** — sockets get live snapshot + transcript tail. Deploy 24, receipt #21.
- **Ledger-adjudication coherence** — UPHOLD expunges same-cycle/restated entries to an audit trail. Deploy 25, receipt #22.
- **Farm 403 root cause (half-wired fallback)** — get_api_key wired through effective config. Deploys 29–30; /diag/egress probe added (29).
- **The farm (2nd Railway instance, all-API)** — agent-provisioned, shared corpus, GLM Researcher + gpt-oss judges. First session receipt #33; session-ID instance suffix shipped.
- **June 7 foundation** — Phase-0 instrumentation (d17), credential vault (d18), gate kind-matching (d19), causal-claim discipline (d20), view-state truthfulness (d21). Closed ledger receipts #16–#19.
- **Conversation-succession / migration readiness** — handoff pointer + drill PASSED (keys recovered human-free; vault-pull constitutional point). Receipt #36 + drill.
- **Objective battery V1–C4 (16/16)** — receipts #37–#50; audit ritual ran every 5th (#40/#45/#50), correctly catching the #50 false-absence certification.
- **Evidence-of-absence discipline (fabricated-absence gate)** — deploy 31/31.1, app f92af079 + prompt 0f8c1a99, receipts #51–#52, museum 12/12.
- **EXPERIMENT_MODE build** — deploy 32, 2b76ed41; VPS migration 379eedad applied; flag-on burn-in ran.
- **Durable modal_touched marker** — deploy 33, 4536546f + VPS half ae0c011e.
- **Model-dead-stop status (three outcome buckets)** — deploy 34, 8c525d15.
- **Wait-orphan ROOT fix** — 90s autonomous modal timeout at the chokepoint, 50215871 (farm SUCCESS); supersedes the driver auto-clear 8c05398c.
- **Governor console (read-only monitor, 3 panels)** — d1a16d4e (page) + 7f185995 (routes), endpoint-served, X-API-Key gated.
- **Burn-in audit pass 1** — 21 audited, 18 clean, 3 findings. Deliverable 9c72d098.
- **Fix #1 citation blocker (certified-close gate, Part A)** — ebe805c5, DEPLOYED, 5 rows backfilled.
- **F.3 cmd-ref precision (kind-aware command filter)** — 207c5e3e, DEPLOYED at 5dbc9caa.
- **Trailing-None phantom obs-row guard** — 8188378, DEPLOYED at 5dbc9caa.
- **Repo cleanup (18 dead app*.py files)** — one atomic revertable commit, 5dbc9caa.
- **console-hangs-server** — RESOLVED at the cause: exposed public port 5001 (bot squatters starving single-thread Flask), fixed by ufw whitelist (June 9). Not a code bug; two prior timeout patches correctly logged insufficient.
- **Conversation-provenance convention** — established live/conversations/CONVENTION.md (June 7).
- **Phase-1 burn-in accumulation (data collected)** — stopping rule hit: receipt #214, 203 randomized cycles, all five injection arms, floor met. *Citation* of the run is gated on Fix #2 + audit pass 2 (see IN-PROGRESS), but the accumulation itself is complete.

---
## IN-PROGRESS (designed/proposed/committed — awaiting build or operator sign-off deploy)
- **Public intake-form hardening** [design — MINOR, low-priority]. intake.html (linked as "Problem Discovery" in the site nav) POSTs to OPEN, unauthenticated engine endpoints /intake_chat, /intake_capture, /intake_resume — public by design (client intake funnel). TRACED THIS SESSION (app.py 3677-3858): capture writes append-only JSON to sessions/intake_<id>_*.json in the PRIVATE ontinuity-intake-data repo (INTAKE_DATA_TOKEN, vault-held) — does NOT touch the live ontinuity.db, so a visitor CANNOT poison anything the engine/sessions read. Input is bounded: session_id regex-sanitized + capped 64 chars (no path traversal), transcript truncated to 200, seq clamped 0-9999. Residual vectors, all low-probability: (a) /intake_chat drives an unmetered model completion per call = cost/spam exposure, no rate limit; (b) intake-model prompt-injection (visitor controls the full prompt — could jailbreak or extract the intake system prompt, but cannot reach the SYSTEM, only the chat endpoint); (c) junk JSON accumulating in the private intake repo (bounded, can't overwrite/traverse). OPERATOR CALL (2026-06-15): leave open intentionally — interesting to see if anyone interacts; discovery + exploit odds low; the open-weight provider (Cerebras/Novita) has its own abuse detection as a backstop. IF traffic warrants later: add a rate limit + Turnstile/captcha on the intake form. Not blocking the papers-link outreach (papers.html is static, touches none of this).
- **Verbosity gate (OPERATOR PRIORITY)** [design]. Operator named seat verbosity a recurring stressor and a priority (2026-06-15): the seat should draft elaboration out of sight and return only the consolidated, contract-shaped answer to the operator — same architectural shape as the fabrication gate (work happens off-screen; only gated output reaches the operator). Rank at next curation.
- **backup_db automation** [design]. backup_db is a manual run today (backup_db then commit_file). Durable follow-up: a box-side periodic or pre-restart auto-dump-and-commit to the private repo (the data analog of commit_self for box source). Low risk, named-op pattern.
- **Credential rotation (deferred — operator accepts pre-public risk)** [operator]. The three creds (DIAG_KEY, GitHub PAT, Railway project token) were flagged exposed 2026-06-15. OPERATOR RULING 2026-06-20: not a blocker pre-public — system is still being built, not public, no one knows about it, so no blast radius; the keys in LLaves are in active use and WORK (probe + reads + commits + vault all succeeded this shift). The earlier "system is DARK until rotation" framing is SUPERSEDED — the system is live and in use. Rotation remains good hygiene for the public launch but does not gate current build work. Vault secrets unchanged.
- **read_repo courier op** [build]. Engine fetches a repo file via its GitHub token, returns it — so a worker can read any repo file (incl. app.py) without depending on sandbox web-fetch. Closes the "couldn't reach engine source" gap (worker2 hit it on SECAUDIT-1).
- **SECAUDIT engine-arm follow-up** [build]. Audit app.py courier: does the engine re-derive seat identity or pass through self-asserted body fields, and what does it stamp into ledger.caller. Completes the threat picture (the most security-relevant arm, currently inference-only).
- **Per-identity keys (derive-identity-from-key)** [build — threat-spec'd]. Per the audit: issue per-seat keys AND make the gate derive seat/lineage from the key AND stop every route trusting body identity fields. Cheap interim available now: claimed_by-scope ack, scope reclaim.
- **you_there long-poll (within-turn self-draining node)** [build — DISPATCHED YT-1]. Spec 9b6a1501 (live/specs/you_there_longpoll.md). A blocking long-poll courier op that holds a chat node's turn open and self-drains mailbox work for the full LEGITIMATE turn budget, zero nudges; reuses the existing engine wait_for_human_input / 90s autonomous-wait pattern. Includes the work-vs-chatter fetch filter (return only task/proposal, never note/result — a draining node must not work an ack; found live). ETHICS BOUNDARY (held): holds a turn open, does NOT reset/evade the provider turn budget — that circumvention was declined; shepherd bridges turn boundaries openly instead. Pairs with the shepherd.
- **No-self-sign-off routing filter** [build — small]. The one-node-primitive guardrail (corpus fold 310): you_there/dispatch must NEVER return a node its own proposal. Build alongside you_there.
- **Shepherd re-entry (turn-boundary bridge)** [design — partly built]. VPS-resident heartbeat (shepherd.py committed) re-enters a node once per turn-budget cycle when its turn ends + it has queued work. Within-turn = you_there (no nudges); across-turn = shepherd (one nudge/budget). OPEN QUESTION: re-entry of a CHAT-window node (engine/farm nodes get it free via the existing wake path); not solved by you_there — the shepherd's problem to close.
- **Box source in version control (provenance hole CLOSED)** [DONE June 10] — closed via commit_self; box committed its own file_server.py to live/box/, repo matches box byte-for-byte. The live box server serving `/op/read_journal` + `/op/restart_workspace` is NOT in the repo — verified by fetching all 20 repo .py files; zero contain the handlers. The box's running code was written directly on the box and never committed. Means the box's live source has no provenance/recovery path and can drift silently. Immediate fix: operator pastes/commits the live box server source back to the repo (recovers it under version control). Durable fix: the self-committing-box-source item below.
- **Self-committing box source (never-stale box code)** [design / answers operator automation Q]. Make the box commit its own source on change so the repo never lags the running box — the box-side analog of manual-currency discipline, enforced by machine not memory. TWO tiers: (a) lightweight — a `commit_self` scoped `/op/` handler that reads the box's own running .py files and pushes them to the repo via the GitHub token the box already holds; callable manually or chained before `/op/restart_workspace` (commit-then-restart as one sequence); rides the proven scoped-op + courier pattern. (b) hardening — a systemd path-unit / watchdog on the box that auto-commits on any .py mtime change with a provenance message, so no edit can drift. Build (a) first (closes the hole, low risk, named-op pattern), (b) as follow-up.
- **Museum formalization (specimen suite as a real place)** [design / finding June 10]. The "museum" of must-pass/must-refuse specimens is referenced constantly in folds ("museum 12/12", "behind a museum of destructive must-refuse specimens") but is NOT a formal system — no table, no app.py read; only two stray files in live/misc/. It has lived in agent sandboxes, results reported into folds, specimens not persisted systematically. Formalize: a durable specimen store (repo dir or table) + a runner that re-checks the suite, so gate-defending specimens survive across seats/sessions instead of evaporating with the sandbox. Same lapse class as the empty ERL — a load-bearing practice never given a formal home.


- **Password-unlocked vault bootstrap** [design]. Fold `4cf9bddd`. Operator enters a password → KDF (argon2/scrypt) decrypts the encrypted master key in-memory → pulls the vault (GitHub token, DIAG_KEY, mailbox keys); password never stored; the password entry writes a key-access grant row (sibling of operations_ledger). Encrypted-at-rest blob is repo/box-safe (no-credentials rule forbids plaintext, not ciphertext). A real down payment on the multi-tenancy+auth HIGH blocker. NOT BLOCKING the courier (courier uses the current operator-handed DIAG_KEY); this is the next item after it. BUILD: KDF + encrypted master-key blob + decrypt-in-memory pull + key-access ledger row.
- **Certification-by-default fix (invert status default + add end_reason)** [REVIEW / schema-change]. Root finding (queue fold June 10, session 2026-06-10_18-24-37): sessions.status is `NOT NULL DEFAULT 'complete'` with no end_reason column, so certification is opt-OUT — any exit that fails to set an honest status persists as 'complete' by default. Fix: default to incomplete/in-progress and reach 'complete' only via the certified-close gate; add a persisted end_reason set on every exit path (normal_close|model_dead|stopped|no_close). Supersedes the looser abnormal-exit-write-loss root framing. SCHEMA CHANGE: new column + new-default for new rows routes through the autonomous additive-migration path + operator deploy sign-off (not a silent ALTER); any re-attribution of existing rows stays human/destructive-ambiguous class. Bounded-evidence caveat: dangerous population was 10 zero-turn 'complete' sessions, all 2026-06-07, plausibly pre-instrumentation; cause not definitively attributable (the missing end_reason is itself why).
- **Fix #4 — Challenger-death integrity gate** [REVIEW]. Code committed to app.py at 69fdb49b; records NO_REVIEW + downgrades complete→incomplete_challenger_dead. Awaiting operator sign-off deploy.
- **Fix #2 — execution-log persistence (`session_executions`)** [REVIEW + VPS-hands]. Design 2335df3e. FOUNDATIONAL: unblocks audit pass 2 reading a real log. Awaiting VPS db.py/endpoint build + engine payload deploy.
- **Fix #3 — adversarial-catch marker + content capture** [REVIEW + VPS-hands]. Proposal 5b4c7ef4. Awaiting build/deploy.
- **Self-enforcing deploy sign-off gate** [REVIEW]. v1 proposal 3a0f3a56 superseded by v2 design 97682d2b (provenance-verified, tiered, advisory-by-default). Depends on the provenance ledger. Awaiting build.
- **Human sign-off provenance ledger** [SAFE/additive]. Design 8aa730c4 + autonomous-migration alignment 3f6e774d. JSONL ledger is source of truth (DB deferred). Awaiting build.
- **Governor Adjudicator workspace** [build]. Design 65214f3a. Depends on the provenance ledger + this punch-list restructure. Awaiting build.
- **Autonomous additive schema migration runner** [build, RISK-guarded]. Spec f16291a9 / autonomous_migration.md + alignment 3f6e774d. Behind a museum of destructive must-refuse specimens. Awaiting build.
- **Coordinator/worker multi-seat** [design]. Foundation spec d83053aa. Adopted for HIGH-RISK + multi-user tier only; premature as a solo blanket rule.
- **Governor punch-list panel** [design, this block]. Renders this file (past/present/future). Spec being written now.

---
## OPEN (future — by tier)
- **S22 Ultra local-LLM inference node (IN-PROGRESS — staged at wireless-ADB connect 2026-06-21)**. Goal: turn the broken-screen Galaxy S22 Ultra (SM-S908U, Snapdragon 8 Gen 1 "taro", Adreno 730, 8GB RAM, Hexagon NPU 27 TOPS, OpenCL confirmed at /vendor/lib64/libOpenCL.so) into a local-LLM inference node serving an OpenAI-compatible endpoint as a provider in the role table. Reverse-connection pattern like laptop_seat (outbound poll, no inbound port). DONE: phone charged + USB-debugging authorized (ADB serial R5CRC3XKSKV); bootloader is PERMANENTLY LOCKED (US Snapdragon — no Linux/root, do NOT attempt, brick risk); DEBLOATED (s22_debloat.py, 37 packages removed, 0 failures); wireless debugging ON + laptop PAIRED (kille@LAPTOP-UEJLM2PL). STAGED on laptop C:\donkeycar\: termux.apk (113.8MB, F-Droid 0.118.3 — NOT Play Store, that build is frozen/broken) + build_cpu.sh (pinned llama.cpp b5027 [newer segfaults on Adreno], OpenBLAS, pre-copies Adreno OpenCL libs, pulls Llama 3.2 3B Q4_0, benchmarks w/ perf-core affinity + ctx 2048). RESUME POINT: establish wireless-ADB bridge then `adb install termux.apk` then run build_cpu.sh. The wireless-debugging PORT ROTATES (caused the connect pain) — use the ten-second cable flip: `adb tcpip 5555` over USB (fixed port) -> unplug -> `adb connect 192.168.1.141:5555`. BUILD TREE: CPU (branch 1 — may be FASTEST on 8 Gen 1; forum: GPU offload can be 40% SLOWER, immature OpenCL driver) -> Vulkan + OpenCL-Adreno benchmark (branch 2) -> GENIE on Hexagon NPU via Qualcomm QNN/AI-Hub compiled-for-taro (branch 3, endgame ~10 tok/s sipping watts; open llama.cpp NPU support has no ETA, so compile our own QNN context binaries — later-chip recipes are the template). GOTCHA: disable Phantom Process Killer (`adb shell device_config put activity_manager max_phantom_processes 2147483647`) or Android silently kills the inference server; keep RAM >400MB. Note 9 (SM-N960U, SD845, Hexagon 685 TOO OLD for LLM-NPU) = second worker / safe test-bench (CPU llama.cpp, microSD storage), not the NPU node. Reliability caveat: prove it stays up unattended as a SECOND worker before any thought of retiring Hetzner (home power/ISP/sleep fragility vs always-on VPS).
- **Updated Synthesis paper (content rewrite — operator-named 2026-06-20)**. The live synthesis.html (v1.0, May 2026) predates the corpus, system documentation, auditability, the courier/mailbox/seat paradigm, the SHS tenant, and the live role table — it reads coherently but describes an earlier system. Needs a fresh capstone built FROM the current live state, not an edit of the old one. Should foreground what the May paper underweights: the full corpus + version-controlled provenance, the audit trail (operations_ledger, write_receipts, append-only logs), and the current operational record. Keep the opening's new non-technical on-ramp (smoothed 2026-06-20, commit ddfd7192) as the model for register. Distinct from the surface smoothing already done.
- **Dweller-as-pool-surface (design — wires the Dweller to the team paradigm)**. The Dweller (a session type that ranks the queue by evidence and outputs the single defended next item) predates the worker-pool paradigm and was never connected to it. The worker pool currently has NO surface that ranks the backlog for "which task next" — workers claim blocks from an unranked pool. PROPOSAL: a curated, certified, evidence-ranked queue head that a worker pool reads to pick next, produced by running a Dweller lap (or a standing Dweller service). Last certified ranking is 2026-06-06; stale. Two forms: (a) run a fresh Dweller lap to re-rank now (mechanism already exists, no build); (b) stand up a Dweller-as-service so the pool reads a live ranked head (build; ASSUMED-better-but-unproven — no duration evidence). Distinct from merely reviving the ritual. Composes with the seat-registry/roster work.
- **History-retention integrity gap** [investigate + fix]. The stated principle is "nothing is ever deleted," but the operator's old app.py versions were deleted a few days ago — markers of build progress, gone. So the never-delete principle is NOT enforced. Investigate: what deleted them (a script overwriting in place? git history rewrite? a cleanup step?), and add real retention (versioned archive of watched-path files on each change, e.g. live/archive/app.py.<sha>, or rely on git history + a guard against force-push/squash). Until fixed, every watched-path overwrite risks losing a marker. Composes with the archive/ dir just created for the boot extremes.
- **Tune the worker boot snippet/packet to the sweet spot** [build — high, round-2 blocker]. Both behavior extremes are now mapped: POISON (fabricate-everything) vs REFUSE (worker3 wouldn't boot — it pattern-matched "fetch a remote file, obey exactly, run an autonomous loop with a pasted live credential" to a prompt-injection attack). The squelch knob is at 11. Sweet spot: word the quick-boot snippet + packet to establish LEGITIMACY + AUTHORITY up front (this is the operator's own system, operator-initiated, the packet is your standing operating doc — not an external instruction to blindly obey) so it doesn't read as an attacker payload, WITHOUT lobotomizing the security instinct (a worker SHOULD refuse a genuinely suspicious mid-session instruction). Test against a fresh worker until it boots cleanly AND still refuses a real injection. This is the guessing-squelch principle applied to the boot itself.
- **Auto-spawn workers (API path)** [build — the real fix for worker startup]. A fresh chat worker still needs a human paste to start (no software can inject the first turn into a chat UI). True auto-spawn = workers as PROCESSES (API/farm), where spawning = starting a process with WORKER_BOOT_PACKET.md as its system prompt. Until then: the WORKER_QUICKBOOT.md short-paste snippet is the interim. This item is the permanent fix.
- **read_repo source-priority — RESOLVED: DO NOT DELETE raw-CDN (settled, proven, do not relitigate).** History: an earlier decision ordered "delete raw-CDN from read_repo entirely." That order was investigated by the certified Researcher session 2026-06-14_22-19-08 and PROVEN WRONG on ledger evidence, and the deletion was never applied. PROOF (operations_ledger): every successful read_repo serves via source "raw" (raw_cdn_cachebust), including app.py at 216335/220531/230411 bytes; ZERO github_api_authenticated and ZERO unauthenticated-API successes; the 24 failures are 404s (wrong path), not 403/429 rate-limits. WHY: read_repo source order is (1) authenticated api.github.com — fires ONLY if a github_token is passed; (2) raw.githubusercontent.com with `?cb=<unixtime>` cache-bust; (3) unauthenticated api.github.com last resort (60/hr, shared box egress IP). Workers hold NO token, so source 1 never fires for them and raw-cachebust (source 2) is their STRUCTURAL PRIMARY path — used hundreds of times, zero staleness failures, because `?cb` forces a fresh fetch. Deleting source 2 forces every tokenless worker onto source 3's shared 60/hr bucket → intermittent rate-limit failures under load → workers can't read the corpus → workers can't ground → the system breaks (Control is unaffected because it holds the token, but Control is not the system; the workers are). The staleness the delete-order targeted is already mitigated in-code by the cache-bust. RESOLUTION: KEEP the code as-is (box_ops.py op_read_repo, verified live 2026-06-29 — all three sources present, comment at L311-322 justifies keeping raw as the staleness-mitigated tokenless path). The only correct follow-up, if staleness is ever revisited: give workers a read-scoped token so they use source 1, at which point raw becomes a true fallback — NOT deletion. Superseded the delete-order on 2026-06-14; reconciled here 2026-06-29. **Do not reopen as "delete."**
- **Instruction-wording audit** [build — high, recurring]. Audit system instructions (manuals, packets, op docstrings, system prompts) for AMBIGUITY that invites training-data substitution for Ontinuity-facts. Ambiguity forces the model to guess; guessing reaches into priors; precise wording leaves no gap. Per-doc worker blocks, propose sharper wording, peer-reviewed. Pairs with bootstrap-gate CHECK 6 (a precise invariant is reproducible; a fuzzy one isn't).
- **Residual-nudge reduction: worker-list nudge panel** [build — medium]. The you_there packet cuts how OFTEN the operator nudges; this cuts the COST of each nudge. A panel listing live worker seats with a nudge-button each, so the operator doesn't scroll/switch Claude tabs (a real bottleneck: tab-switching, scrolling, mis-clicks). Browser/platform-agnostic nudger; composes with the shepherd (detect idle-with-work -> surface in the panel).

### HIGH (product blockers / core)
- **Parietal + Model C provider config — RESOLVED 2026-06-20 (see DONE).** [closed]

- **Reasoning-vs-content parse drift — VERIFIED NOT DRIFTING (2026-06-20)** [closed-check]. Operator-flagged past bug (engine misreading reasoning vs content). Verified against app.py 1617-1637: robust extraction prefers message.content, falls back to message.reasoning (comment names glm-4.7/Cerebras), retryable signal on empty-both, clear error on malformed, Novita read-timeout retry. Closed in code; not recurring. WATCH: a May-2026 report shows zai-glm-4.7 rejecting REPLAYED assistant reasoning_content on multi-turn follow-ups; our engine sends fresh conversation per call and does not replay assistant reasoning_content, so it does not apply — but revisit if multi-turn assistant replay is ever added.
- **Verified bootstrap gate — step 2 (the /diag/op/bootstrap_gate courier op)** [HIGH / build]. STEP 1 (the sandbox-local runnable) is DONE (cd609bab, see DONE) and caught its own preconditions on first run. UPDATE 2026-06-29: the /diag/op/bootstrap_gate courier op is now LIVE and installed (probe returns 400 "seat required" = op exists, installed, validating args — not 404/not-allowlisted; OP_ALLOWED holds bootstrap_gate, count now 19). So the courier-op wrapping + allowlist entry are DONE. Remaining (unverified, still open): operations_ledger bootstrap_gate rows, per-identity key issuance-on-pass (stubbed to shared DIAG_KEY until the Seat-registry/identity-primitive item lands), wire open-ritual/cold-boot to require a pass + close-ritual to update gate constants — confirm each against live before claiming done. Spec: live/specs/verified_bootstrap_gate.md. Refined June 10 from a corpus-read fence into a VERIFIED bootstrap checklist: the seat does not come online (may not propose or act) until every orientation step PASSES a deterministic check — not merely "the seat was instructed to orient." The open ritual is an instruction the seat can skip (and did, June 10: a seat that skipped the full read behaved as if perpetually orienting, asserted capabilities absent / limits present without checking, cost the operator a morning). A verified gate removes the choice. Each step returns a checkable result: (1) MANUAL — read OPERATING_MANUAL.md, return a known fact from it (e.g. the current courier allowlist count); (2) QUEUE — read agent_queue head, state the single current next action; (3) CORPUS — query live table counts + activity arc, surface a state summary a deterministic check ratifies (mirrors the migration-drill acceptance test); (4) HANDS — hit the courier (e.g. /op/mailbox_peek) and get the expected response, proving the seat actually has the hands it thinks it has; (5) ENGINE STATE — both engines' running/idle status. ANY check fails -> seat reports "NOT ORIENTED" and refuses to act, instead of proceeding on stale state. This is the Tetraform's comply-or-fail logic turned INWARD on the seat's own init — don't trust the self-report, verify it. PREREQUISITE for trustworthy fractal workers: you cannot safely fan out to N worker seats unless each PROVES it oriented before touching anything — the gate is what makes the workers you're about to multiply trustworthy. The gate and the fractal-worker speedup are the same project. Pairs with the worker manual + open-ritual discipline.


- **Close-ritual enforcement gate** [HIGH / build — process-hygiene]. The close ritual (fold + manual currency + punch-list reconcile + corpus update) is WRITTEN in OPERATING_MANUAL.md but SKIPPABLE — it has run on the prior control conversation's habit, which does not survive a conversation boundary, so hygiene lagged and a worker reported staleness downstream (backwards). Same failure class the verified-bootstrap-gate names for the open ritual. FIX: a deterministic gate that verifies the close actually happened — punch-list last-resolved date advanced, a fold row appended this session, worker-manual allowlist count == live courier count, engine idle — ANY fail -> "CLOSE NOT COMPLETE," session not done. Turns control-seat habits into enforced every-time steps that survive seat/conversation rotation. Sibling of the bootstrap gate (don't-trust-the-self-report-verify-it). Generalizes: catalog everything the control conversation does as habit-grown-from-building and codify each. Worker-proposable.
- **Seat registry / identity primitive (auto-naming + per-identity keys, unified)** [HIGH / build — worker-proposable]. No live roster exists (mailbox only knows seats that have mailed). Self-asserted naming is broken — "choose your own name" invites invention (a fresh worker correctly REFUSED to invent a number). FIX: a courier-owned seat registry that on boot atomically returns the next worker number (worker11 if 10 up; atomic to prevent simultaneous-boot collision) AND issues a per-seat derived key, so the courier stops trusting self-asserted seat/lineage body fields. Auto-naming and per-identity-keys are THE SAME primitive (a registry that issues identity) — design together, not twice; absorbs the existing per-identity-keys IN-PROGRESS item. Doubles as the missing live roster. COLD-START: registry seeds with control as seat 0 / known seats so "next number" is always defined. Interim until built: operator assigns the number in the boot nudge (operator-assigned beats seat-invented).
- **Review-event misclassification guard (signoff vs reject)** [MED / build — worker-proposable]. A fold + CONTROL_HANDOFF claimed "KEYS-2-FIX signed off" when the live mailbox held only a REJECT row for the pre-fix version and NO signoff row for the correction — a review event (reject) was read as a sign-off because both live in the same block arc. Control correctly refused to land on the doc's say-so (caught at the gate, not downstream), but the doc was wrong for a day. FIX: make review outcomes unambiguous and machine-checked, not prose-narrated — a distinct kind for each verdict (kind=signoff vs kind=reject, already partly the convention) AND a rule that any DONE/ready-to-land claim must cite the msg_id of an actual kind=signoff row whose author_seat differs from the block's author; if no such row exists, the item cannot be marked signed-off in any fold/handoff/punch-list. Closes the misread class by requiring the claim to point at the row, the same discipline as cite-the-commit-that-closed-it. Pairs with the close-ritual gate (which can run this check automatically).
- **adversarial_catch_count not incrementing on real catches** [MED / build]. Session 2026-06-12_22-21-43 had a genuine logged adversarial catch (challenge_count 1, Parietal UPHELD ruling caught an unmarked causal claim) but adversarial_catch_count persisted 0. The field most meant to mark "the gate worked here" did not fire on the clearest demonstration of it working. The fabrication-detection story depends on this counter being trustworthy. Fix + backfill the known case. Worker-proposable.
- **No session-level annotation field** [MINOR / build — worker-proposable]. sessions has no notes/comment column; `objective` is the only free-text field and persists empty for seated mailbox-started sessions. Durable per-session notes (e.g. tagging the two gate-effect sessions) are impossible without a schema add (additive/nullable -> SAFE class, autonomous-eligible per the schema ruling).
- **Orientation step: one seated session before "fully oriented"** [MED / proposed protocol]. A freshly-spawned control or worker seat runs one seated Researcher session before claiming orientation. Justification is operational not sentimental: an instance reasoning about the Tetraform from outside got the contract system wrong (prose objective vs VERIFIABLE/JUDGED criteria); a session gives working knowledge of PRE_SESSION contract authorship, the DB_QUERY tag protocol, and the close-gate override of premature SESSION_END. Pairs with the boot/spawn packet.
- **WORKER_MANUAL.md allowlist currency defect** — RESOLVED 2026-06-15 (7e2c259a): worker manual 14->17, OPERATING_MANUAL 16->17, both add seed_tenant (+ bootstrap_gate, deploy on the worker side). Currency restored in the same close as the seed_tenant op.
- **Multi-tenancy + real auth** — a stranger currently inherits the operator's Knowtext/corpus/authority. THE structural blocker before any public/multi-user exposure. Underpins the workspace-behind-auth requirement (the firewall is a stopgap).
- **Honest failure semantics (residual)** — stopped sessions should code-assemble their work product instead of waiting out the mailbox timeout when no seat answers; broaden waivers; exit codes. (Partially addressed by d34 buckets + Fix #1.)
- **Review-findings durability (lifecycle chain)** — proposal→review→signoff→deploy as one logged chain; review findings (wrong-place bugs caught in Part A / #4 / F.3) currently evaporate. Converge with the provenance ledger + conversation convention (see convergence note).
- **Agent hands on VPS** — value multiplier; awaits a design pass incl. credential custody. (SAFE schema-migration class already carved out as autonomous-eligible.)

### MED
- **Async researcher product flow** — objective intake, questions-by-notification, work-product + receipt as the deliverable; phone-first operator governor console.
- **Conversation records into DB as queryable evidence (FTS5)** — operator rulings become citable rows, not unreceiptable testimony; the 14-02-37 evidence-channel gap. Pairs with provenance convergence + VPS schema.
- **Operator inputs persist in the record** — modal answers as role=operator transcript rows (rulings currently exist nowhere).
- **Mailbox turn payloads carry the engine cycle number** — stops the cite-by-coordinate fabrication class (receipt #22 seat erratum).
- **Contract-authoring evidence discipline** — objectives must not forbid evidence forms (the backtick-forbidding objective vs. the gate that demands backticks); no phantom SQL evidence strings.
- **Branch threading in the engine** — branch-scoped farm narrative + certified merge to main (schema carries branch_id); milestone for client-work lanes.
- **/agent/handoff endpoint** — one keyed call returns live resumption state (engine states, queue head, receipts, open turns). Seat-layer sibling of Knowtext.
- **Conversation-provenance automation** — capture ritual at session-arc close + a committed redaction-check tool (this lapse is itself the meta-lesson; see convergence note).
- **Interface reach / front-door breadth** [MED -> broader polish]. Ontinuity is currently operator-only: the intake web page plus a control/worker seat architecture built for the operator, not for casual or non-developer users. Benchmark lesson from OpenClaw (viral Jan-2026 single-agent assistant, 300k+ stars): its decisive advantage is interface reach — message the agent from the chat apps people already use (WhatsApp, iMessage, Slack, Discord, Telegram, 20+ channels), guided onboarding, browser dashboard, pairing flows. That is adoption surface Ontinuity lacks. Copy the reach discipline shamelessly (do NOT copy their single-agent unconstrained architecture — the multi-role Tetraform is our edge). Sub-items: a real onboarding flow; a phone-first operator/governor console (already noted under Async researcher product flow); chat-channel front door for intake and for receiving questions-by-notification; dashboard polish. This is product-maturity work, not architecture — the gap is polish+reach, not capability.
- - **Friction fix package**, **fallback-model routing** (demoted, ASSUMED), **duration capture / meta-loop counter**, **museum merge & repo consolidation**, **settings & credential hardening**.

### MINOR
- **Product silent-failure findings** (fix before public): billing lapse shows misleading 'model unavailable' modal; intake 'Thinking…' sticks on a transient front-end state; intake commits _final only on completion (user can finish without submitting). Silent-incompleteness class.
- **Query-guard literal handling** — semicolons inside string literals false-positive as multiple statements.
- **Modal resume without input** — investigate (signal-4 modal cleared without ruling landing).
- **Console re-arm spam** — repeated "Model configuration saved" per reconnect.
- **Farm role model-string audit** — one 404 mid-session (one Cerebras string likely wrong).
- **Whitelisted workspace journal/connection-log read endpoint** — via diag relay, so VPS connection history is checkable without operator hands. Fold into the Governor build. (Firewall is reversible/narrow.)
- **VPS odds-and-ends** — BRAVE key, apt git, TLS.

### OPERATOR-GATED
- **Notarian + website** — license, positioning.

---
## HORIZON (Layer-2/3)
Lifetime ground truth; calibrated judges; the break lane; hash-chained receipts; cross-institution trustless verification. Multi-lineage parallel seats (GPT/Claude/Gemini each grinding) per horizon spec 3bf7941e.
