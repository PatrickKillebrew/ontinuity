# AGENT QUEUE — curated by Dweller Lap 1 (session 2026-06-06_22-15-52, receipt #12, CERTIFIED)
Curation decisions defended under the glass; this file is their mechanical application.
Protocol: contract the HEAD ACTIVE ITEM ONLY; close or amend on the record; fold learnings
back via /agent/queue_update; RE-READ before contracting the next item. Dweller laps
periodically rebuild this list; append between laps, curate during them.

## STANDING RULES (cumulative)
- Never deploy during a live session; /diag/engine check is mandatory before any commit to a watched path.
- The queue path (live/agent_queue.md) is UNWATCHED by design — queue commits never trigger deploys.
- Tokens, credentials, and judgment modals are human; initiation and work are agent; the operator is the fuse.
- Lineage is honest everywhere: HARNESS:MODEL in seats, Assisted-by in commits, started_by in sessions.
- No credential-bearing file may live in a path any push tool can reach.

## ACTIVE — certified order (receipt #12)
1. **Fallback-model routing.** ERRATUM (June 6 evening, operator challenge): the certified rationale attributed the two dead starts to provider timeouts; the evidenced cause was the pre-session-questions/stale-panel seam (fixed, deploy 16) — the Parietal responded normally both times. The 67-minute session gap is real; the cause stated in receipt #12 is withdrawn. This item's true support is the June 5 and prior history of provider timeouts, which stands on its own. Rank #1 is annotated as resting on corrected evidence; re-curation under the corrected record is warranted before contracting. Per-role fallback chains, engine-side, museum-testable.
2. **Agent hands on VPS (Claude Code or any capable agent).** Operator direction set June 6. Multiplies throughput of everything below; seat protocol is agent-agnostic by design. Needs a design pass: scope, credentials custody, audit trail.
3. **Frontend view-state truthfulness.** Stale completion panel concealed pre-session questions (contributed to both dead starts); artifact rows accumulate. Promoted from cosmetic by receipt evidence.
4. **Friction fix package (certified by receipts #11).** Loop-state counters into Friction input; rationale must cite row features; reject empty rationales; gate signal-4 on a real UPHOLD; define the 0/1 boundary on explicit row-level features (distributions interleave — 38% of signal-0 rows carry nonzero challenges).
5. **Settings & credential hardening.** Covers the whitelist-wipe class AND token custody (merged): config saves must be receipted and non-destructive; no secrets in pushable paths, tokens via env/config only.
6. **Honest failure semantics (merged: waiver broadening + exit codes).** Deterministic exit-1 environment failures become waivable-structural; push_to_github.py exits nonzero on error (a false PASSED misled receipt #10's session).
7. **Museum merge & repo consolidation (absorbs epoch-stamping, repo cleanup).** test_f3.py off the laptop into the repo with today's specimens; single-point-of-loss closed.
8. **Duration capture.** Elapsed-ms on model calls and executions; AR-001's founding wound; Kaplan-curve feedstock.
9. **Notarian launch + website refocus (merged).** Highest external value; gated on operator decisions (license, positioning) — not contractable until decided.
10. **VPS odds-and-ends.** BRAVE_API_KEY (SEARCH_REQUEST currently dead), apt git, Caddy/TLS.

## LAYER-2 HORIZON (the institution verifies itself)
Lifetime ground truth; calibrated judges; the break lane; hash-chained receipts. Layer-3 named: cross-institution trustless verification.

## CLOSED LEDGER (compressed; receipts #5-#12, June 6)
Mailbox seat (10) · challenge-routing fixes (11) · self-start + lineage (12) · queue I/O (13) · DB_QUERY evidence channel + STOP-wake (14) · json import (15) · console buffer + questions-to-starter (16) · PyGithub/push path proven end-to-end · exposed-credential incident closed · friction audit CERTIFIED with new interleaving finding · first Dweller lap (this curation).
Evidence inventory and day's score preserved in git history of this file (sha 08abe6ff and ancestors).


## ERRATUM LEDGER
- Receipt #12 (Dweller lap 1): the #1-ranking rationale asserted an unverified cause ("provider timeouts") contradicted by the same day's postmortem; caught by the OPERATOR, not by the glass. Lesson promoted to a queue item-class: judged-only criteria can certify unhedged causal claims. Fix direction: causal assertions in-session require either an evidence channel or a mandatory ASSUMED marker, with the Challenger prompt instructed to hunt unmarked causality.


## PROBE 1 AMENDMENTS — June 6 evening (session 2026-06-06_22-35-20, receipt #13, CERTIFIED "UNDECIDABLE")
**FINDING (major): the behavioral corpus measures no behavior.** All metric columns (model_a/b word, hedging, certainty counts) sum to ZERO across all 154 observations — instrumentation never wired. Every behavioral claim must source from session-log text analysis outside this table until repaired. The R²=0.91 sigmoid's evidence necessarily lives outside this corpus.
**NEW item (HIGH, promoted by finding): Phase-0 instrumentation repair** — populate metric columns at write time from actual output text; acceptance: nonzero sums next session; museum fixture test. Certified spec in receipt #13.
**NEW item: Phase-1 causality experiment** — randomized exogenous signal injection (EXPERIMENT_MODE flag, Bernoulli coin flip, computed/injected/randomized columns, pre-registered metrics, permutation analysis, placebo check, n≥200 randomized cycles). Full certified protocol in receipt #13. The Psychology-of-AI-Data thread's next empirical step.
**NEW item: contract-gate kind-aware matching** — bare `DB_QUERY` evidence tokens are unmatchable (gate matches SQL text only); receipt #13's session required a transparent gate-marker query to close. Matcher should match execution kind OR detail.
**PROBE VERDICT:** the architecture certifies calibrated uncertainty and demands rigor of it (uphold forced the experiment to full spec, not the conclusion to overclaim). Epistemic-health probe passed.
