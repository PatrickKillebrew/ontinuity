# PROSE-TO-CODE WALK — how the doctrine and app.py make one mechanism (2026-09-15)
*The operator asked for the doctrine and the code to be read TOGETHER, end to end, instead of parts strung together. Sources read this session in full for the claims below: ONTINUITY_MASTER_SYSTEM_MAP.md §1-4, §20-21; ONTINUITY_CONTINUITY_MECHANISM.md §1-8; ONTINUITY_TOTAL_INDEX.md (Projenius + Easter eggs); STATE_OF_ONTINUITY.md §4-9; prompts/projenius_system.txt; prompts/knowtext_extraction_prompt.txt; app.py (agent_start 3860, pre_session_then_start 4707, run_pre_session 2529, run_session_loop 2856, contract_close_check 1411, apply_upheld_challenge 1287, record_execution 1754, check_execution_claims 1940, run_distillation 2676, run_parietal_distill 2649, run_projenius_orient/synthesize 1508/1524, write_erl_ledger 1113, write_session_to_workspace 917, agent_queue_read/update 3895/3921); db.py (established_results, projenius_ledger_operations, insert/confirm/retract API); live/box/workspace_db_endpoint.py (/api/session, /api/ledger, retraction insert). Each row names the file:function so a stranger can re-walk it.*

## 1. THE TWO DOORS, AS THE CODE HAS THEM
| Doctrine | Code | Note |
|---|---|---|
| MODE A intake: a client's problem becomes structured data (map §1) | /intake_chat (4-phase interview, prompts/intake_system.txt) -> [WORKSPACE_STATE] -> /intake_capture (private repo) -> new_project (per-project Knowtext/ERL rows+files) | LIVE except the WORKSPACE_STATE->first-Knowtext bridge (the one confirmed-open seam, map §20) |
| MODE B direct session: "the operator does ongoing work — design, planning, building — WITH memory continuity" (map §1) | operator+seat boot -> **/agent/start {objective, project_id, branch, start_fresh}** (mailbox-key auth; "initiating work and doing work are the same trust grade; judging is not") -> pre_session_then_start | THE DESIGN DOOR IS A SESSION. The chat window is the CONTROL SEAT that starts, steers and receives it. |

## 2. THE MODE B LIFECYCLE, PROSE NEXT TO CODE
| Phase (doctrine) | Code path | What actually happens |
|---|---|---|
| INJECTION at open: "the boot auto-loads the Knowtext" (CM §6.1); ORIENT "before Parietal PRE_SESSION" (Projenius prompt) | pre_session_then_start: knowtext=load_file(session_knowtext_path()); if not start_fresh: run_projenius_orient(obj, knowtext) -> ORIENT fed the ERL file (session_erl_path) + Knowtext Open Questions (get_open_questions_context) | The ERL is READ here, into the session's priming. start_fresh (F.7) skips ORIENT so a fresh session is not contaminated (CM §6.3 contamination rule, in code). |
| THE CONTRACT: "a gate refuses to let a session close until output matches a contract fixed before the session" (STATE §4) | run_pre_session(objective, orient_context): call_parietal("PRE_SESSION") with the WHITELIST of runnable commands ("VERIFIABLE criteria may only cite commands that can actually pass"); returns (refined_objective, needs_answers, contract). If needs_answers: the Parietal's authorship QUESTIONS go back to the STARTER (captured_mailbox_researcher_turn kind=pre_session_questions) and run_pre_session_with_answers authors the contract | The contract is AUTHORED BY THE SYSTEM from the operator's raw objective, after a clarifying exchange with the starter, and FROZEN before work. Criteria have KINDS: VERIFIABLE (evidence = a command that must appear PASSED in the execution log) and JUDGED (the Challenger's call). This is the "state the problem -> distill -> contract" phase change, built. |
| THE CYCLE: "Researcher produces; rejected_claims injected every cycle; execution_log = ground truth" (map §3) | run_session_loop: Model A -> extract_ledger_entry (ESTABLISHED:/RESULT: lines -> session_ledger); CODE_TEST executions -> session_ledger + record_execution (F.3 ground truth); Model B challenge -> Parietal ADJUDICATE; on UPHOLD: apply_upheld_challenge -> extract_rejected_claim + expunge_overruled_ledger (entries move to rejected_claims + expunged_ledger); check_execution_claims on Researcher output each cycle | "Established" inside the session = survived the Challenger. Expunged entries are never silently deleted (audit trail). |
| CLOSE GATE: "CLOSE REFUSED — contract criterion unmet" | contract_close_check(): deterministic walk of VERIFIABLE criteria against the execution log (kind-aware matching); JUDGED criteria left to the Challenger; SESSION_END refused with the unmet list; Fix #1 then gates the persisted outcome: 'complete' only with a SESSION_END tag, else 'incomplete_no_close' / 'incomplete_challenger_dead' | Two gates at close: the contract (deterministic where checkable) and the certification (evidence of a real close). |
| DISTILLATION at close: "Projenius DISTILL fires at close" (CM §6.1) | run_distillation_with_timeout(run_parietal_distill) first (Parietal DISTILL, 2649); run_distillation (2676) = the standalone knowtext_extraction_prompt through the projenius model as the other path; validate_knowtext_response; save session_knowtext_path(); github_push_knowtext | The seven fields are written per project/branch (structural isolation: "no parameter lets a session name another project's file"). |
| SYNTHESIZE: "promote Delta Log results into the ERL; retractions with GROUNDS" | run_projenius_synthesize(delta_log, knowtext): correction_history from session_ledger entries containing 'retract'; call_projenius("SYNTHESIZE") with the current ledger; write_erl_ledger writes the COMPLETE returned ledger to erl_<slug>.txt (truncation guard) + github_push_erl | The ERL is WRITTEN here and only here. Qualification is the prompt's rule (no contradiction, survived challenge, falsifiable, not process metadata). The DB API for it (insert/confirm/retract + projenius_ledger_operations) exists in db.py and is not called — the FILE is the ledger of record (index §20: file-primary, DB as the LEDGER_QUERY substrate later). |
| PERSISTENCE: "structured evidence a session produced" (CM §1A) | write_session_to_workspace -> build_session_payload -> POST {box}/api/session (X-API-Key) -> workspace_db_endpoint: sessions, session_transcripts, knowtext_versions, challenge_events, retraction_events (from expunged_ledger), model_call_envelopes, session_reproducibility_manifests, session_executions, write_receipts | The relational layer is the queryable index of what a session did. |
| THE CONTRACT-QUEUE LOOP: "sessions amend the work queue ON THE RECORD; next contract authored against the amended list" (map §1) | /agent/queue (agent_queue_read: reads AGENT_QUEUE_PATH from GitHub) and /agent/queue_update ("the fold-learnings-back step": a GitHub commit with provenance; the path is UNWATCHED so a queue commit never triggers a deploy) | **AGENT_QUEUE_PATH is live/agent_queue.md — the SAME file the control seat folds into.** Engine sessions and seats already share one narrative ledger. |

## 3. THE ANSWER TO "HOW COULD A DESIGN SESSION USE THE ERL WITHOUT AN ADVERSARIAL SESSION?"
It reads it; it does not write it. The ERL is a committed corpus file per project/branch (erl_<slug>.txt, pushed by github_push_erl) and a box endpoint (GET /api/ledger, "used by Projenius LEDGER_QUERY and ORIENT"). Any seat can read_repo it today. WRITING the ERL happens in exactly one place — SYNTHESIZE after a session whose results faced the Challenger — and that is by design: ESTABLISHED means "survived challenge." A design session that wants project memory written for it IS a Mode B session: /agent/start with the objective, ORIENT reads the ERL in, the contract is authored, the cycle runs, DISTILL and SYNTHESIZE write Knowtext and the ERL out. The chat seat's own continuity is the corpus rituals (fold, punch list, record, handoff), now gated. There is no shortcut, and there should not be one.

## 4. THE INCONGRUENCE, NAMED
The V2 frame (record 2026-09-13d) says the design door is the chat window and verification is a gate the design session calls. The map says Mode B is a session. Both are right once the layers are named: the CHAT SEAT (control) drives Mode B; a matter's MEMORY (Knowtext + ERL) is produced by the sessions the seat starts; the seat's own memory is the corpus rituals. "Calling the verification gate" IS /agent/start with a load-bearing objective. The seat's rituals and the engine's contract-queue loop were built by the same hand to the same shape (STATE §4 "gate-and-contract"; CM §7 "discipline -> mechanism"), and they meet in agent_queue.md. What this week did was make the SEAT layer's rituals mechanical (L1-L5). What the seat still lacks is what the engine already has for a session: an authored, frozen contract with VERIFIABLE/JUDGED criteria checked deterministically at close. That, and nothing new, is L6.5.

## 5. WHAT THIS CHANGES IN THE PLAN (proposed; operator rules)
- L6.5 MIRRORS THE ENGINE'S CONTRACT, it does not invent one: `contract_set` = the seat's PRE_SESSION (items from the punch list; each criterion VERIFIABLE — evidence is an ops-ledger row / commit sha — or JUDGED — the operator's ruling); close_gate CHECK 1 = the seat's contract_close_check (deterministic walk of VERIFIABLE criteria against operations_ledger; JUDGED left to the operator); the seat's FOLD = its /agent/queue_update (same file, same loop).
- The ERL stays research-door-written. `orient` and `status` READ it (file or /api/ledger) for the matter alongside folds/records/punch list. No design-door writes into established_results (correction already recorded in two_doors_one_memory.md §3).
- Research-door refinements stay where the corpus put them: parse SYNTHESIZE into the db.py API (index §20 / erl_decision Option A) and wire LEDGER_QUERY as a courier op (index "revised build picture" step 4).
- The seat that wants a matter's memory written runs a Mode B session for it. The packaged product's "remembering seat" = the gated seat rituals (v1) + the ability to start Mode B sessions on its matters (already live on the operator engine; per install it needs the five role providers configured — Section C3 of the runbook lists them).

## 6. ADDENDUM (second check, same session) — THE SEAT INSIDE THE SESSION, THE DRIVER, THE WRITE PATH, THE TIERS
The first walk covered the session lifecycle and left out how a CHAT SEAT gets INSIDE a session. That is the mechanism packaging needs most (STATE §8.2: "give any user's AI instance the ability to sit in the SEAT ORIENTATION").
| Doctrine | Code | Note |
|---|---|---|
| "PUTTING A CLAUDE IN THE RESEARCHER SEAT (mailbox-seat mode — the real harness-from-inside) ... NOT chat role-play (that's theater, no gate fires)" (OPERATING_MANUAL) | get_effective_config("model_a").url startswith "external" (app.py 2209/2823) -> mailbox_researcher_turn (2344): posts {kind, turn_id, system, conversation, cycle, session_id} to external_mailbox and WAITS (MAILBOX_TIMEOUT_S); GET /mailbox/turn?mailbox_key (3960) returns the pending turn; the seat answers (kind researcher_turn or pre_session_questions); the engine resumes, and the Challenger, Friction, Parietal ADJUDICATE, F.3, the contract gate and the certified close all fire ON THE SEAT'S WORDS | THIS is how a user's own AI sits in the seat with real gates. Proved 2026-09-03 (ChatGPT in the Researcher seat, real HTTPS mailbox crossing, gated close; the fix between laps was a staffing config change only). The chat window becomes the Researcher of a Mode B session; the design work IS the session. |
| "THE TWO SESSION START MODES" | /agent/start -> started_by=external-mailbox (needs a DRIVER answering the mailbox or the session hangs at 'waiting for agent' and NEVER writes); /start_session (dashboard) -> started_by=dashboard, loop runs internally | A packaged install running Mode B needs either the resident driver or the seat driving by hand. |
| "THE RESIDENT DRIVER (the shepherd)" | systemd `ontinuity-burnin` (/opt/ontinuity/burnin_resident.py) on the VPS answers the external mailbox and drives sessions to close; ALWAYS-ON; exactly one driver per farm | Part of the box package for Product 2 (not in the six-file remembering-seat box). |
| "THE WRITE PATH" | build_session_payload -> POST {box}/api/session ONLY on normal close; DIED/STOPPED sessions lose their payload (fail-soft /tmp/failed_session_<id>.json); >=2 cycles before SESSION_END | Why a hung mailbox session persists nothing. |
| "MODALS in autonomous vs attended" | MODAL_TIMEOUT_AUTONOMOUS_S = 90 (2409): with no operator present a modal self-clears after 90s (a checkpoint, not a question) | Attended (dashboard) vs autonomous (mailbox/farm) is a real behavioral fork. |
| Product tiers | db.py users.plan ('personal'|'free'|'pro'|'team') + feature_flags JSON; storage_configs per-user backend (endpoint_url, credentials) | The multi-user product is SCHEMATIZED; a per-operator install runs on defaults (0 storage_config rows). |

### 6.1 THREE SEAT ORIENTATIONS, ONE PRIMITIVE
- CONTROL seat (chat): drives the corpus and starts sessions; gated by bootstrap_gate / orient / close_gate (this week).
- RESEARCHER seat (chat via the external mailbox): does the matter's work inside a Mode B session; gated by the frozen contract, the Challenger, F.3, the certified close.
- WORKER seat (chat via the seat mailbox): builds and reviews in blocks; gated by no-self-signoff and the two-party deploy chain.
Same shape everywhere: the seat cannot certify its own work; a gate with a ledger row decides.

### 6.2 PACKAGING IMPLICATIONS (filed to the-package as runbook corrections)
- v1 (the remembering seat, CURRENT_STATE "IS: context management and pausability; IS NOT: Tetraform runtime") = the CONTROL-seat orientation with its rituals now mechanical. Its memory is the corpus (folds/punch list/records/handoff). That is what install two proves.
- Product 2 (the engine instance) adds Mode B: the user's AI in the RESEARCHER seat. Per-install requirements the runbook's C3 does NOT yet list: MAILBOX_KEY; MODEL_A url = "external" (the user's AI occupies the seat) OR a provider; a Challenger of a DIFFERENT lineage (MODEL_B_*); PARIETAL_* (contract author + adjudicator + DISTILL); PROJENIUS_* (ORIENT/SYNTHESIZE — the matter's long memory); optionally MODEL_C_*; the resident driver on the box for autonomous runs. Install two has none of these set.
- The continuity mechanism's "product = automate the two Knowtext frictions" is Product 2's claim; v1's claim is the gated seat. Both true, different layers. L6.5 (the seat's contract) is built to the ENGINE's contract shape so that when Product 2 lands on an install the two loops are one loop.

### 6.3 NOT YET WALKED IN CODE (named so the boundary is honest)
Governor (relay/routes/html), Model C friction signal + Knowtext modes, four-stage pipeline stages 2-4 (design only), the sanitizer (separate app), the intake->first-Knowtext bridge (confirmed-open seam), burn-in / EXPERIMENT_MODE internals, work_product/final_synthesis generators, session_series/branch forking. None of these changes §3-§5; each is a bounded read if a build touches it.
