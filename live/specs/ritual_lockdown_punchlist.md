# RITUAL LOCKDOWN — PUNCH LIST (sequenced; one item per session-of-work; each closes on a ledger fact)
*Companion to live/specs/ritual_lockdown_plan.md (the inventory + gap audit). This file is the WORK ORDER: items are sized to be built,
installed, verified, and folded individually so no seat has to hold the whole program in context. Rule for every item: DONE means a
ledger row a stranger can walk to, a deliberately-failing call that returns the right failure message, and the fold citing both.
Test bed: install two (ontinuity-two). Operator install gets everything in one promotion step (L9) through the two-party chain.
Method: the documented new-op two-step — repo commit (box/ + app.py OP_ALLOWED) -> box install (write_file + restart_workspace) -> engine deploy -> verify through the courier.*

Last resolved: 2026-09-15 (L1–L6 DONE; next L4b).

## DONE
- **L3a — portable bootstrap gate (found missing while starting L3).** `gate.py` was hardwired to the operator install (public raw URLs of the operator corpus, MAIN+FARM URLs, floor 307, literal op count) and was not in the box package at all, so `bootstrap_gate` could not run on install two. Now `box/gate.py`: engine/farm/corpus/floor from the box config with the rule ABSENT = THIS INSTALL HAS NONE (two bugs came from falling back to the operator's values); corpus reads token-per-call for private repos; CHECK 1 compares the manual's "Allowlist (live, N ops)" to the LIVE probe, not a literal. Install two's manual gained that line through its own courier (d95784a). Commit 3b71fb1.
- **L3 — `seat_sessions` rows.** LIVE on install two (3b71fb1; box install of file_server/box_ops/box_boot/gate + config engine_url; restart). Acceptance read from the ledger via /diag/api/query: `bootstrap_gate` oriented on all six checks -> row 3da89a81 (seat, role, lineage, started_at, closed_at NULL); a session-tagged `orient` row joined to it; two local boots -> two rows. `describe` now reports the common fields (seat, seat_session_id). L9 note: set corpus_session_floor=307 in the operator box config at promotion.
- **L6 — `commit_file` hardening.** LIVE on install two (cfb3c56; box install only, no allowlist change). message required (400 otherwise); identical bytes at ref -> logged no-op via a locally computed git blob sha (no empty commits, the f86c20d class); dry_run reports create/update/unchanged and commits nothing. Verified locally (one real commit d38fffb on install two's README, repeats no-op) and through the courier; ledger rows say unchanged / dry_run / committed.
- **L5 — `close_gate` op.** LIVE on install two (9b3fd4b; allowlist 24). PRE-BUILT FOUND: staging/close_gate.py (worker11/worker22, June) on the operator box, never committed or installed — now box/close_gate.py with the ten gaps applied; original preserved at museum/staging/close_gate_2026-06_worker11_worker22.py. Acceptance both directions, all facts from repo/engine/ledger: NEGATIVE — fresh session, no work -> PUNCH-LIST/CONVERSATION/QUEUE-FOLD/HANDOFF/ORIENT fail in one run with the spec's messages, precondition on missing token, refusal with no session; POSITIVE — install-two session 1e9cdf0a (boot -> orient -> six courier commits inside the window: manual to 24, record 2026-09-15c citing session shas, fold with one **NEXT, handoff, punch list) -> dry_run 9/9 -> real close: closed:true, session closed_at set, key revoked, revoked key refused ('seat key revoked' + row), double close refused. Ledger row 120 is the close. Found while building: install two's handoff has no 'Updated <date>' header, so every 'updated this session' test now means 'committed since started_at' (commit list), never a header string.
- **L4 — per-identity keys end to end.** LIVE on install two (39de9ad; engine-two relay forwards X-Seat-Key). Courier acceptance from the ledger: X-Seat-Key + body 'impostor' -> caller `seat:control (auth)` joined to session 53dba362; bogus key -> `seat key unknown` + op_refused row; local: revoked -> `seat key revoked` + row; registry hashes only with revoked_at/reason. Negative evidence kept: before the relay deploy, the same calls logged 'impostor' and ignored the bogus key. Found pre-built and reused: registry, authenticate_identity, register_seat_key, _caller_seat preference. Actually missing: the forwarded header, real issuance, revocation, refusal path.
- **L2 — `orient` op.** LIVE on install two (64e8722; box install + engine-two deploy; allowlist 23). Verified through its hrough unchanged (relay auth stays `X-Diag-Key`); (b) `bootstrap_gate` mints a real key on pass, registers its hash bound to {seat, lineage, seat_session_id}, returns it once as `key_issuance.key` with `key_kind: per_identity`; (c) `_authed_identity` reads `X-Seat-Key` first, `_ledger_begin` records the authenticated caller + `seat_session_id` from the key's registry entry (no body field to forget); shared-key calls keep working, logged `unattributed`. Acceptance: two boots -> two keys -> two distinct authenticated callers on their ops; a body claiming another seat is logged under the key's identity; an op with a revoked key is refused with the right message.

- **L4b — mailbox routes on key-derived identity [L4; BEFORE L9].** per_identity_keys.md build-order step 3 is NOT done: seat_mailbox send/fetch/ack and `_noself_predicate` still trust the body seat. Migrate them to `_identity_seat()` (key first, body only in shared-key mode) so acceptance Q1/Q2/Q3 in that spec close; the worker mechanism's guarantees (no-self-signoff, ack ownership) depend on it. Acceptance: a node holding worker2's key cannot send as control, cannot ack another seat's claim, cannot review its own proposal by relabeling.

- **L7 — one seat per install: contention made mechanical [L3]. Applies to role=control ONLY — workers are many-seats by design.** `bootstrap_gate` refuses (or warns, operator's call) when a `seat_sessions` row is open with `closed_at:null` from a different caller identity, returning the open session's seat + started_at. The operator can force with `takeover:true`, which closes the stale row with reason `takeover` (logged). Acceptance: second boot while a session is open returns the contention message; takeover produces two rows with the join visible.

- **L8 — packet v3 + manual currency [L3, L5].** Boot ends with `bootstrap_gate` (booted == oriented:true + a session_id); close ends with `close_gate` (closed == closed:true). Manual: COLD-BOOT gains "the sandbox shell is sh (dash); use python3 for anything beyond a one-liner"; SCOPED OPS points to `describe` instead of listing bodies; OPEN ritual names `orient`; CLOSE ritual names `close_gate`. CONTROL_QUICKBOOT v3 stays platform-agnostic (v2.1 is the base). Acceptance: a fresh seat on install two boots and closes with the gates' returns as the only evidence of boot and close.

- **L9 — promote to the operator install [L2–L8 all DONE on install two].** One batch through the two-party chain: box source (live/box/*) + app.py OP_ALLOWED + packet + manual. Proposal by one seat, signoff by a distinct seat, deploy MAIN via `deploy`, box via write_file+restart. Acceptance: the operator install's probe shows the new ops; its next real close runs through `close_gate`.

- **L6.5 — session contract + receipt [L5, L6; before L8]. Seed: live/specs/session_contract_receipt.md; integration map: live/specs/deep_dive_findings.md.** seat_sessions gains `project`; punch list adopts the governor panel spec's format contract + stable item ids; `contract_set` marks items IN-PROGRESS under the session (ledger row); close_gate CHECK 1 reconciles those items; `status` = the LIVE /agent/handoff extended with open seat_sessions + contract items + last close result (the receipt; the resume boot report). The user never authors a contract; the seat sets it at distillation; no contract -> close notes exploration-only.

## PARKED (not lockdown; packaging items tracked in the-package PUNCH_LIST)
- Section D seed must include live/conversations/CONVENTION.md.
- Section D docs platform-agnostic sweep (manual /home/claude + sandbox; paradigm/rubric model names).
- V2-E (assertion rule at the reflex level) — deferred by ruling until v1 ships.
